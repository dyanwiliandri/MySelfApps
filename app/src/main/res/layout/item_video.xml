<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="#FFFFFF">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="200dp" android:layout_marginBottom="8dp">

            <ImageView
                android:id="@+id/video_thumbnail"
                android:layout_width="360dp"
                android:layout_height="160dp"
                android:src="@drawable/gallery"
                android:scaleType="centerCrop"
                android:layout_gravity="center"
                android:clipToOutline="true"
                android:contentDescription="Video Thumbnail" />

            <ImageView
                android:id="@+id/video_play_icon"
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:layout_gravity="center"
                android:src="@drawable/play"
                android:contentDescription="Play Video" />

        </FrameLayout>

        <TextView
            android:id="@+id/video_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Judul Video YouTube Keren"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="#50545B"
            android:maxLines="2"
            android:ellipsize="end"
            android:layout_marginBottom="4dp" />

        <TextView
            android:id="@+id/video_subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Channel Creator • 5:30"
            android:textSize="14sp"
            android:textColor="#50545B"
            android:alpha="0.7"
            android:maxLines="1"
            android:ellipsize="end" />

    </LinearLayout>
</androidx.cardview.widget.CardView>